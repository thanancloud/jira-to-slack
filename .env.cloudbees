# CloudBees CI Environment Configuration
# Note: In CloudBees CI, these values are set via workflow and secrets
# This file is for reference only - DO NOT commit actual credentials

# Jira Configuration (Set as CloudBees Secrets)
# JIRA_URL=https://cloudbees.atlassian.net          # Set in workflow
# JIRA_EMAIL=<set-as-secret>                        # Set as secret: JIRA_EMAIL
# JIRA_API_TOKEN=<set-as-secret>                    # Set as secret: JIRA_API_TOKEN

# AWS Bedrock Configuration (Set in workflow)
# AWS_REGION=us-east-1                              # Set in workflow
# BEDROCK_MODEL_ID=us.anthropic.claude-sonnet-4-5-20250929-v1:0  # Set in workflow

# AWS Authentication (Handled by OIDC)
# AWS_PROFILE is NOT needed in CloudBees CI
# Authentication is handled via OIDC role assumption
# Role: arn:aws:iam::ACCOUNT_ID:role/infra-claude-bedrock-ci

# Slack Configuration (Currently disabled)
# SLACK_WEBHOOK_URL is not used (Slack communication is commented out)
